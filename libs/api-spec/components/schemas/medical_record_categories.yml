_id:
  type: object 
  properties:
    id:
      type: number
      example: 1

_main:
  type: object
  properties:
    name:
      type: string
      example: Outpatient

response:
  allOf: 
    - $ref: '#/_id'
    - $ref: '#/_main'

request:
  allOf:
    - $ref: '#/_main'
